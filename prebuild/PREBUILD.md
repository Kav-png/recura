# Pre-build assets (Wed 22 – Fri 24 July)

This folder holds everything that must exist BEFORE doors open Saturday. The block files in ../blocks assume these are done.

- `parse-prompt.md` — the production prompt for Block A. Iterate it against all three letters in a notebook Thursday; whatever survives goes into `/api/parse` verbatim.
- `letter-1-hf.html` — THE demo letter (heart failure — matches the HF/COPD beachhead, no stroke). Open in a browser, print it, photograph the printout under venue lighting Saturday morning. Contains one deliberate danger flag: new HF regimen (furosemide, sacubitril/valsartan) while ibuprofen remains on the repeat prescription list (NSAID → fluid retention + blunted diuretic effect, risks decompensation).
- `letter-2-clean.md` — COPD discharge letter, zero danger flags. Proves the system doesn't cry wolf on the second beachhead condition (Block F option 2).
- `letter-3-nightmare.md` — stress test only. Never demo unless it parses perfectly.
- `whatsapp-template.md` — exact template text to submit to Meta TONIGHT (Wednesday). Two retry cycles depend on early submission.
- Show `letter-1-hf` to the surgeon friend during the pitch: "is this interaction real, does this look like a real discharge letter, what call answer would be negligent to mishandle?" Fold his corrections back into the letter and the prompt.

All patients fictional. No real data anywhere.
